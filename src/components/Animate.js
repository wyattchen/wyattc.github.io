import "./Animate.css"
import Design from "./Design"

function Animate() {
    // this should be run only once per application lifetime
    return (
        <Design id='particlesDesign'>
        </Design>
      );
  }
  
  export default Animate;